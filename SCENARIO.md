# Track 3 시연 시나리오

상태: 사용자가 송금 한도 시나리오로 진행하도록 확정했다.

## 보여주려는 문제

고객이 송금을 요청했지만 기관이 거절했다. 실제 송금이 발생하지 않았으므로 송금 내역만으로는 요청과 거절 이유를 확인할 수 없다. 기관이 나중에 이유를 바꾸거나 기록을 숨겨도 고객과 감사자가 알아낼 수 있어야 한다.

## 등장인물과 예시 규칙

- 고객: 요청 내용을 자신의 키로 서명하고 접수 증거를 보관한다.
- 기관: 사전에 공개한 규칙으로 판단하고 요청·입력·규칙·결과를 연결해 서명한다.
- 외부 기록 주체: 기관과 별도로 요청 접수와 판단 기록의 증거를 보관한다.
- 감사자: 별도로 확보한 신뢰 기준과 증거 파일로 검증한다.

예시 규칙은 “1회 송금액이 100만 원을 초과하면 거절”이다. 150만 원 요청은 거절하고 50만 원 요청은 승인한다. 실제 자금 이동 없이 판단만 시연한다.

## 정상 시연

1. 시연 시작 전에 기관·고객·외부 기록 주체의 공개키와 적용 규칙을 감사자에게 제공한다. 검증 대상 파일이 자기 공개키를 제시하는 것만으로 신뢰하지 않는다.
2. 고객이 금액과 고유 요청 식별자를 포함한 요청에 서명한다.
3. 판단 전에 외부 기록 주체가 요청을 접수하고, 고객이 접수 증거를 받는다.
4. 기관이 규칙을 실행하고 요청 해시, 입력, 규칙 버전 및 해시, 승인·거절 결과, 사유를 연결한 판단에 서명한다.
5. 판단을 외부 기록에 추가하고 고객에게 독립 검증에 필요한 증거 묶음을 제공한다.
6. 기관 서버를 중지한다. 감사자가 증거 묶음과 미리 확보한 외부 기준만으로 서명, 기록 포함 여부, 입력에 대한 규칙 실행 결과를 확인한다.

## 공격 시연

| 상황 | 기대하는 결과 |
| --- | --- |
| 거절 사유나 금액을 사후 변경 | 서명 또는 기록 증거 불일치 탐지 |
| 기관 DB에서 판단 삭제 | 고객·외부에 보관된 증거로 원래 판단 확인 |
| 감사용 목록에서 판단 한 건 제거 | 고정된 감사 범위의 외부 기록과 비교해 불완전한 목록 탐지 |
| 접수된 요청을 판단 목록에서 처음부터 누락 | 접수 목록과 판단 목록을 대조하여 기한 경과 후 미처리 탐지 |
| 고객이 요청 사실을 부인 | 고객 서명과 요청 내용을 확인 |
| 기관이 거절 사실을 부인 | 기관 서명과 판단 내용을 확인 |
| 서명된 판단이 공개 규칙의 결과와 다름 | 기록 진위와 별개로 규칙 불일치 표시 |

## 검증 경계

- 완전성은 외부에 접수된 요청과 지정된 감사 범위를 기준으로 한다. 어디에도 제출되지 않은 요청의 존재를 증명하지 않는다.
- 판단 기한 이전의 요청은 대기 중이다. 기한 이후 판단이 없으면 미처리이며, 거절이 있었다고 단정하지 않는다.
- 한 건의 포함 증명만으로 전체 기록에 누락이 없다고 결론 내리지 않는다.
- 규칙 재실행은 제공된 입력과 결과의 일치를 검증한다. 외부 현실의 사실 여부나 기관 내부의 숨은 동기까지 증명하지 않는다.
- 외부 기록 주체는 로컬 모의 로그로 구현하고, 감사 범위는 사전 전달된 체크포인트까지로 고정한다. 판단 기한은 접수 후 60초다. 키는 역할별로 다르게 생성하지만 한 프로세스가 보유한다. 로컬 모의 구성을 실제 독립 운영이나 온체인 배포로 표현하지 않는다.

## 참고 코드와 후속 순서

`reference-projects/tessera/README.md` 및 `docs/design/README.md`에서 추가 전용 로그, 순번, Merkle 트리와 체크포인트 구조를 검토했다. 요청 접수·판단을 연결하는 애플리케이션 로직은 별도로 필요하다.

`reference-projects/linera-protocol/README.md`에서 외부 앵커 후보인 체인 애플리케이션 구조를 확인했다. 이번 시연은 로컬 외부 로그를 사용하며 체인 연동은 하지 않는다.

`DEVELOPMENT.md`에 따라 시나리오 협의 → 요구사항 명세 → 테스트 스위트 명세 → 실패 테스트 → 구현 → 재실행·리팩토링 순서로 진행한다. 각 테스트 완성 시 `AIM.md` 충족 여부를 확인한다.
