# 요구사항

사용자가 확정한 송금 시나리오를 대상으로 한다. 실제 송금은 하지 않는다.

| ID | 요구사항 | AIM 대응 |
| --- | --- | --- |
| R1 | 고객은 양의 정수 원화 금액, 요청 ID, 기관 ID, 정책 해시를 서명한다. 중복 ID와 잘못된 금액은 거부한다. | 요청자 부인 방지 |
| R2 | 외부 기록 주체가 고객 서명을 검증하고 요청 접수 시각과 판단 기한을 기록한다. | 최초 누락 탐지 |
| R3 | 기관은 100만 원 초과를 LIMIT_EXCEEDED로 거절하고 이하를 승인한다. 요청 해시와 정책 해시, 결과와 사유를 서명한다. 판단을 외부 로그에 기록한 후 증거를 제공한다. | 거절 기록, 기관 부인 방지 |
| R4 | 로그는 순서가 있는 Merkle 트리와 외부 주체의 서명된 체크포인트로 고정한다. | 변조 탐지 |
| R5 | 단건 증거는 요청과 판단, 각각의 포함 증명을 담는다. 독립 검증기는 사전에 제공된 공개키·정책·체크포인트만 신뢰한다. | 독립 검증 |
| R6 | 감사 범위는 체크포인트의 처음부터 size까지다. 전체 로그의 순서·크기·루트를 재검증하고 접수와 판단을 대조한다. 기한 전 미판단은 pending, 기한 경과 후는 overdue다. | 삭제·누락 탐지 |
| R7 | 기관 저장 파일을 삭제해도 고객 증거는 검증 가능하다. 변조, 목록 누락, 최초 판단 누락, 허위 사유, 키 대체, 오래된 범위 사용을 시연·테스트한다. | 시연 목표 |
| R8 | CLI 검증 성공은 종료 코드 0, 증거 검증 실패는 1이다. 네트워크와 기관 파일 없이 실행한다. | 독립 도구 |

## 증거 스키마 v1

서명 대상은 `{domain, payload}`의 정규 JSON UTF-8 바이트다. 객체 키를 정렬하고, 배열 순서를 유지하며, 문자열은 NFC로 제한하고 숫자는 안전한 정수만 허용한다. Ed25519 서명은 base64, SHA-256 해시는 hex다. daryl의 정규 직렬화·해시·서명 분리 아이디어를 참고하되 별도 포맷이며 호환성을 주장하지 않는다.

- request: `version, id, customer, institution, amount, currency, policyHash`
- decision: `version, requestHash, policyHash, outcome, reason`
- envelope: `domain, payload, signature`
- log entry: `index, acceptedAt, deadline, evidence` (deadline은 요청에만 존재)
- checkpoint: `version, logId, size, root, issuedAt`를 외부 키로 서명
- trust: 고객·기관·외부 공개키, 정책, 서명된 감사 체크포인트. 기관이나 증거 파일에서 자동으로 신뢰 기준을 가져오지 않는다.

리프는 SHA-256(0x00 || 정규 entry), 내부 노드는 SHA-256(0x01 || left || right)로 계산한다. 비균등 트리는 가장 큰 2의 거듭제곱 경계로 분할한다. Tessera의 Merkle 로그·순번·체크포인트 아이디어를 차용한다. Tessera 서버 및 Linera 체인 자체를 연동한 구현은 아니다.

## 신뢰 경계

외부 주체와 고객이 기관과 공모하지 않고 공개키를 사전에 안전하게 확보했다고 가정한다. 한 머신의 로컬 데모는 실제 독립 운영을 증명하지 않는다. 전체 감사는 고정된 범위에 대한 것으로 이후 기록의 최신성, 외부 로그의 분기·공모, 외부에 접수되지 않은 요청, 실제 자금 이동, 현실 입력의 진실성은 보장하지 않는다. 기관 서명은 해당 판단의 서명을 증명하며 고객 서명은 요청 사실을 증명한다. 고객이 판단에 동의했다는 뜻은 아니다.
