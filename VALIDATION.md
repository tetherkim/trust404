# 검증 기록

환경: Node.js v24.4.1, macOS. 네트워크 및 외부 패키지 없이 실행.

## TDD 진행

1. 시나리오 확정 후 요구사항과 테스트 스위트 명세 작성.
2. 핵심 테스트 작성 후 `npm test`: 구현 모듈이 없어 실패 확인.
3. 핵심 구현 후 10/11 통과. 한도 경계 테스트의 역행 시각을 발견하여 각 금액을 독립 환경에서 테스트하도록 수정.
4. CLI 통합 테스트 작성 후 `npm test`: CLI가 없어 실패, 핵심 11개 통과.
5. CLI 구현 후 12/12 통과.
6. 유효한 포함 증명 안의 잘못된 서명과 저장 실패 시 응답 방지 검사를 추가. 최종 14/14 통과.

공통 직렬화·서명·스키마 검사, Merkle 경로, 판단 재실행을 공유 함수로 분리했다. 파일 접근은 CLI에 두어 검증 함수가 기관 저장소를 읽지 않도록 구성했다.

## AIM 점검

| 항목 | 확인 결과 |
| --- | --- |
| 거절 발생 시 신뢰 가능한 기록 | 거절을 서명하고 외부 역할 파일에 저장 후 반환. 저장 실패 시 성공 응답 없음 |
| 거절 스키마 | REQUIREMENTS.md에 요청·판단·해시·서명·체크포인트 정의 |
| 독립 검증 도구 | CLI가 증거와 사전 신뢰 파일만 읽음. 기관 폴더 삭제 후 성공 |
| 불변성 | 변경된 금액·사유·서명·로그 순서·중복 탐지 |
| 완전성 | 고정된 범위의 기록 삭제 및 접수 후 기한 초과 미처리 탐지 |
| 부인 방지 | 서로 다른 고객·기관 Ed25519 키. 잘못된 키로 서명한 기록 거부 |
| 사유 검증 | 기관이 유효하게 서명한 허위 승인도 규칙 재실행으로 거부 |

단건 증거 성공으로 전체 범위의 완전성을 주장하지 않는다. 완전성은 `audit` 결과로 별도 확인한다. 외부 접수 이전의 요청, 최신성, 외부 주체의 공모는 검증 범위 밖이다.

## 실제 시연

`node src/cli.js demo demo-output` 실행 성공. 정상 거절, 승인, 전체 감사는 PASS. 사유 변조, 서명 위조, 목록 삭제, 최초 판단 누락, 기관이 서명한 허위 결과는 DETECTED.

CLI 통합 테스트는 별도 Node 프로세스에서 검증하며 기관 폴더 삭제, 파일 누락, 기존 출력 덮어쓰기 방지, 실패 종료 코드도 확인한다. Merkle 경로는 1~17개 리프의 모든 위치에서 검증한다.

실제 분산 운영 및 온체인 통합 테스트는 수행하지 않았다. 이 구현에는 해당 기능이 없다.
